import { TiempoPublicacionPipe } from './tiempo-publicacion.pipe';

describe('TiempoPublicacionPipe', () => {
  it('create an instance', () => {
    const pipe = new TiempoPublicacionPipe();
    expect(pipe).toBeTruthy();
  });
});
